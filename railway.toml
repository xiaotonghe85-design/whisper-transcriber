[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/bin/sh -c 'gunicorn --bind 0.0.0.0:${PORT} --timeout 600 app:app'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
